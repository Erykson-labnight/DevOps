apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "fase2-app.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "fase2-app.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "fase2-app.name" . }}
    spec:
      containers:
        - name: fase2-app
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 80
      imagePullSecrets:
        - name: regcred